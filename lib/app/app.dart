import 'package:flutter/material.dart';
import 'return_zero_app.dart';

class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return const ReturnZeroApp();
  }
}
