import 'dart:ui';

class AppConstraints {
  // Breakpoints
  static const double mobileBreakpoint = 600.0;
  static const double tabletBreakpoint = 900.0;

  // Horizontal "Void" Ratios
  static const double defaultPadding = 24.0;
  static const double foldableMarginRatio = 0.15;
  static const double tabletMarginRatio = 0.25;

  // Typography Scaling
  static const double tabletTextBoost = 1.2;

  // Item Metrics
  static const double itemHeight = 56.0;
}
