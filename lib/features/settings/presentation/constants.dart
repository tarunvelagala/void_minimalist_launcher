class Constants {
  // Choice Toggle Spacing
  static const double toggleHorizontalPadding = 12.0;
  static const double toggleVerticalPadding = 8.0;

  // Choice Toggle Animation
  static const Duration toggleAnimationDuration = Duration(milliseconds: 200);
}
