import 'package:flutter/material.dart';

class SettingsPageSection extends StatelessWidget {
  const SettingsPageSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
